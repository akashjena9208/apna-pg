spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/apnapgdb
    username: postgres
    password: Akash@2002
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB

file:
  upload-dir: uploads

logging:
  level:
    root: INFO
    com.apnapg: DEBUG
